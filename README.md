Crucial Clothing Store — Business Manager

Run the Streamlit app:

```bash
pip install -r requirements.txt
streamlit run frontend/main.py
```

The app stores data in `crucial.db` in the project root.
